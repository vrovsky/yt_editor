{
  "name": "yt-editor-monorepo",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "rust-media-engine",
    "web-editor-ui",
    "editor-backend"
  ],
  "scripts": {
    "build:rust": "npm --workspace=rust-media-engine run build",
    "dev:ui": "npm --workspace=web-editor-ui run dev:app",
    "dev:backend": "npm --workspace=editor-backend run dev"
  }
}
